# Pinned. A collection that floats is the same problem as an unpinned action: the playbook
# that worked last month runs different code today.
---
collections:
  - name: ansible.posix
    version: 2.1.0
  - name: community.general
    version: 11.4.0
