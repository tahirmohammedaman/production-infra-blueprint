apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The topics Job travels with the broker: the application's consumers subscribe to topics that
# must already exist, and the data stage waits for this Job before anything else rolls.
resources:
  - statefulset.yaml
  - service.yaml
  - topics-job.yaml
