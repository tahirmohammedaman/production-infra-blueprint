apiVersion: v1
kind: ServiceAccount
metadata:
  name: api
  namespace: blueprint
# The API talks to Postgres, Redis and Kafka. It never talks to the Kubernetes API, so it
# does not need a token mounted into every pod — and a token that is not there cannot be
# read out of a compromised container.
automountServiceAccountToken: false
